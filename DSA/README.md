| Program File | Category | Time Complexity | Description |
| :--- | :--- | :---: | :--- |
| **`bubble_sort.cpp`** | Sorting | $O(N^2)$ | Repeatedly steps through list, compares adjacent elements, and swaps if out of order. |
| **`insertion_sort.cpp`** | Sorting | $O(N^2)$ | Builds sorted array one item at a time by inserting elements into correct position. |
| **`selection_sort.cpp`** | Sorting | $O(N^2)$ | Repeatedly finds minimum element from unsorted portion and moves it to sorted portion. |
| **`quick_sort.cpp`** | Sorting | $O(N \log N)$ | Divide-and-conquer algorithm partitioning array around a pivot element. |
| **`merge_sort.cpp`** | Sorting | $O(N \log N)$ | Divide-and-conquer algorithm recursively splitting array and merging sorted halves. |
| **`linear_search.cpp`** | Searching | $O(N)$ | Sequentially checks every element in the array until match is found. |
| **`binary_search.cpp`** | Searching | $O(\log N)$ | Searches a sorted array by repeatedly dividing the search interval in half. |
| **`add_two_arrays.cpp`** | Array | $O(N)$ | Performs element-wise addition of two equal-sized integer arrays. |
| **`highest_element.cpp`** | Array | $O(N)$ | Finds the maximum (highest) numerical value in an array. |
| **`reverse_array.cpp`** | Array | $O(N)$ | Reverses the order of elements in an array using two-pointer technique. |
| **`sum_elements.cpp`** | Array | $O(N)$ | Calculates the total sum of all elements in an array. |
| **`sum_even_elements.cpp`** | Array | $O(N)$ | Calculates the sum of only even numbers present in an array. |
| **`stack.cpp`** | Stack | $O(1)$ ops | Array-based implementation of Stack supporting push, pop, peek, isEmpty, and isFull. |
| **`postfix_evaluation.cpp`** | Stack | $O(N)$ | Evaluates Reverse Polish Notation (postfix) arithmetic expressions using a stack. |
| **`queue.cpp`** | Queue | $O(1)$ ops | Array-based implementation of a standard Queue supporting insert, delete, and display. |
| **`stack_class.cpp`** | Stack | $O(1)$ ops | Object-oriented (class-based) implementation of a Stack with dynamically allocated array. |
| **`circular_queue.cpp`** | Queue | $O(1)$ ops | Array-based implementation of a Circular Queue data structure. |
