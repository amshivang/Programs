| Program File | Category / Type | Description |
| :--- | :--- | :--- |
| **`Alphabet_Triangle.py`** | Pattern | Prints an increasing alphabet triangle pattern (A, AB, ABC...). |
| **`Diamond_Pattern.py`** | Pattern | Prints a symmetric diamond shape made of stars (`*`). |
| **`Floyds_Triangle.py`** | Pattern | Prints Floyd's Triangle with sequential numbers ($1, 2 \, 3, 4 \, 5 \, 6 \dots$). |
| **`Inverted_Number_Triangle.py`** | Pattern | Prints an inverted triangle of numbers counting down per row. |
| **`Inverted_Pyramid.py`** | Pattern | Prints an upside-down pyramid using stars (`*`). |
| **`Inverted_Right_Triangle.py`** | Pattern | Prints an inverted right-angled triangle. |
| **`Number_Triangle.py`** | Pattern | Prints a right-angled triangle with column-indexed numbers. |
| **`Palindrome_Number_Pyramid.py`** | Pattern | Prints a palindromic number pyramid (e.g. 1, 121, 12321). |
| **`Pyramid_Pattern.py`** | Pattern | Prints a centered pyramid pattern using stars (`*`). |
| **`Repeated_Number_Triangle.py`** | Pattern | Prints a triangle where each row repeats its row number. |
| **`Right_Triangle.py`** | Pattern | Prints a basic right-angled triangle using stars (`*`). |
| **`armstrong_number.py`** | Math | Checks if an input integer is an Armstrong number. |
| **`calculator.py`** | Math | Simple interactive arithmetic calculator. |
| **`character_frequency.py`** | String | Counts the frequency of each character in a given string. |
| **`check_temp_humidity_input.py`** | Logic | Evaluates temperature and humidity inputs for environmental thresholds. |
| **`command_line_sum.py`** | Math | Calculates sum of numbers passed via command-line arguments (`sys.argv`). |
| **`control_flow_statements.py`** | Basics | Demonstrates `if-elif-else` control flow structures. |
| **`data_types_demo.py`** | Basics | Demonstrates built-in Python data types (`int`, `float`, `str`, `bool`, `list`). |
| **`divisible_by_3_and_6.py`** | Math | Checks whether a given integer is divisible by both 3 and 6. |
| **`euclidean_distance.py`** | Math | Calculates 2D Euclidean distance between two points $(x_1, y_1)$ and $(x_2, y_2)$. |
| **`even_or_odd.py`** | Math | Determines whether an input integer is even or odd using modulo operator `%`. |
| **`factorial_calculation.py`** | Math | Calculates factorial of a number using iterative loop. |
| **`fibonacci_sequence.py`** | Math | Generates Fibonacci terms up to N. |
| **`format_sentence.py`** | String | Demonstrates string formatting using f-strings and `.format()`. |
| **`hello_python.py`** | Basics | Classic Hello World script introducing Python `print()`. |
| **`in_hand_salary.py`** | Math | Computes net in-hand salary after HRA, DA, and tax deductions. |
| **`largest_of_three.py`** | Math | Determines the largest value among three user-provided numbers. |
| **`leap_year_check.py`** | Math | Checks whether a year is a leap year based on Gregorian calendar rules. |
| **`menu_driven_conversion.py`** | Logic | Menu-driven unit converter (length, weight, temperature). |
| **`multiplication_table.py`** | Math | Prints the multiplication table for any given integer. |
| **`narcissistic_number.py`** | Math | Validates Narcissistic (pluperfect digital) numbers. |
| **`operators_demonstration.py`** | Basics | Demonstrates arithmetic, relational, logical, and bitwise operators in Python. |
| **`palindrome_check.py`** | String / Math | Checks if an input number or string reads the same backwards. |
| **`personal_introduction.py`** | Basics | Interactive script printing a formatted student bio. |
| **`predict_data_types.py`** | Basics | Demonstrates `type()` inspection on evaluated expressions. |
| **`print_end_parameter.py`** | Basics | Demonstrates `end` parameter customization in Python `print()`. |
| **`print_sep_parameter.py`** | Basics | Demonstrates `sep` separator parameter customization in Python `print()`. |
| **`profit_or_loss.py`** | Math | Calculates profit or loss percentage given cost price and selling price. |
| **`remove_punctuation.py`** | String | Removes all punctuation marks from a string. |
| **`reverse_digits.py`** | Math | Reverses the digits of an integer using arithmetic division and modulo. |
| **`reverse_string.py`** | String | Reverses a string using Python slicing `[::-1]`. |
| **`simple_interest.py`** | Math | Calculates Simple Interest using formula $SI = \frac{P \times R \times T}{100}$. |
| **`student_details.py`** | Basics | Prompts for and displays student profile data. |
| **`student_grading.py`** | Logic | Assigns letter grades (A, B, C, D, F) based on percentage score. |
| **`sum_of_digits.py`** | Math | Computes the sum of all individual digits in a number. |
| **`sum_of_n_natural_numbers.py`** | Math | Computes the sum of first N natural numbers using formula $\frac{N(N+1)}{2}$. |
| **`sum_of_squares_of_digits.py`** | Math | Calculates the sum of squares of each digit in a number. |
| **`swap_temp.py`** | Math | Swaps two variables using a temporary storage variable. |
| **`swap_three_variables.py`** | Math | Rotates values across three variables ($a \rightarrow b \rightarrow c \rightarrow a$). |
| **`swap_without_temp.py`** | Math | Swaps two variables without using a temporary variable (arithmetic trick). |
| **`type_conversion_sum.py`** | Basics | Demonstrates explicit type casting (`int()`, `float()`) prior to addition. |
| **`valid_triangle_angles.py`** | Math | Validates whether three angles form a valid triangle ($\text{sum} = 180^\circ$). |
| **`variable_memory_address.py`** | Basics | Inspects variable memory addresses using `id()`. |
| **`volume_of_cylinder.py`** | Math | Calculates volume of a cylinder using formula $V = \pi r^2 h$. |
| **`vowels_and_consonants_count.py`** | String | Counts total vowels and consonants in an input string. |
| **`welcome_user.py`** | Basics | Prompts for username and displays a personalized welcome greeting. |
