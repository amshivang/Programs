#include <stdio.h>
int main()
{
    int a, b, sum;
    printf("Enter two numbers to be added using pointers:\n");
    scanf("%d", &a);
    scanf("%d", &b);
    int *p1 = &a;
    int *p2 = &b;
    sum = *p1 + *p2;
    printf("Sum: %d\n", sum);
    return 0;
}