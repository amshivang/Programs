| Program File | Description |
| :--- | :--- |
| **`5digitsum.c`** | Calculates the sum of all digits of a 5-digit integer input. |
| **`Armstrong_Number.c`** | Checks whether a given integer is an Armstrong number (sum of cubes of digits equals the number). |
| **`FirstCprogram.c`** | Introductory C program demonstrating basic formatted output using `printf`. |
| **`add_two_numbers_using_pointers`** | Demonstrates pointer dereferencing by adding two numbers using memory addresses. |
| **`areavol.c`** | Calculates the area and volume of basic geometric shapes (cubes, cylinders, spheres). |
| **`binary.c`** | Converts a decimal integer into its binary equivalent. |
| **`calc.c`** | Menu-driven C calculator supporting addition, subtraction, multiplication, and division. |
| **`digit.c`** | Extracts, counts, and manipulates individual digits of an integer. |
| **`factorial.c`** | Computes the factorial of a given non-negative integer iteratively. |
| **`fibonacci_series_upto_n.c`** | Generates and prints the Fibonacci sequence terms up to a user-defined limit `N`. |
| **`fine.c`** | Calculates library book return late fine based on total days overdue. |
| **`firstprg.c`** | Basic C program structure demonstration printing console messages. |
| **`guess.c`** | Interactive number guessing game using pseudo-random numbers and conditional loops. |
| **`largest.c`** | Finds the largest number among user-provided numeric inputs. |
| **`maxminoffournumber.c`** | Finds the maximum and minimum values among 4 integer inputs. |
| **`money.c`** | Monetary currency denomination calculator breaking down total amount into notes/coins. |
| **`number_name.c`** | Converts numeric digits into their English word equivalents (e.g. 123 -> One Two Three). |
| **`perfectnumber.c`** | Determines whether an input integer is a Perfect Number. |
| **`perfectnumbers.c`** | Finds and prints all Perfect Numbers within a specified range. |
| **`prg.c`** | C syntax exercise program. |
| **`prime_number_till_n.c`** | Generates all prime numbers up to a specified upper bound `N`. |
| **`rfib.c`** | Recursive implementation of Fibonacci term calculation. |
| **`root.c`** | Calculates real and complex roots of a quadratic equation $ax^2 + bx + c = 0$. |
| **`secondprg.c`** | Introductory program demonstrating variable declarations and user input via `scanf`. |
| **`squarecircle.c`** | Computes perimeter/circumference and area for square and circle shapes. |
| **`swap_two_numbers_using_pointers.c`** | Swaps values of two variables in-place using pointer dereferencing. |
| **`tempCodeRunnerFile.c`** | Temporary C execution script. |
| **`triangle.c`** | Checks triangle validity and classifies it as equilateral, isosceles, or scalene. |
| **`variablesize.c`** | Uses `sizeof` operator to print memory allocation sizes for C primitive data types. |
